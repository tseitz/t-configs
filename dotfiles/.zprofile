# The following lines were added by Docker Desktop to add commands to your PATH.
# Keep these two marker comments. Docker Desktop looks for them on every start and
# re-adds its own block if they are missing. The path uses $HOME instead of the
# literal path Docker wrote, so this file works on any machine.
export PATH="$PATH:$HOME/.docker/bin"
# End of Docker Desktop section.
