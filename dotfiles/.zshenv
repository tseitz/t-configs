# .zshenv — sourced for all zsh sessions (interactive and non-interactive)
# Use for environment variables that must be available before .zshrc loads.
# Keep this minimal; most config belongs in .zshrc.
