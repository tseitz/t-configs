# Personal, every repo on this machine — never shared with a team, never
# committed anywhere. See core.excludesFile in .gitconfig.

# mattpocock-skills domain-modeling glossary (CONTEXT.md / CONTEXT-MAP.md).
# No team repo I work in has adopted this workflow, so these stay local-only
# regardless of which repo they land in.
CONTEXT.md
CONTEXT-MAP.md

**/.claude/.cc-writes/

**/.claude/settings.local.json

# nvim falls back to the cwd for its log when its state dir is unwritable, as in Claude's sandbox.
nvim.log
